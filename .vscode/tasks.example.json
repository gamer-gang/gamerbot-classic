{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "compile",
      "command": "yarn",
      "args": ["build"],
      "options": {
        "env": {
          "PATH": "NVM_BIN_PATH:$PATH"
        }
      }
    }
  ]
}
